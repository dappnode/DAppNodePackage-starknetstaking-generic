version: "3.5"
services:
  staking:
    build:
      context: staking
    environment:
      PROVIDER_HTTP_URL: "http://execution.starknet.dncore.dappnode:6060/v0_9"
      PROVIDER_WS_URL: "ws://execution.starknet.dncore.dappnode:6061/v0_9"
  ui:
    build:
      args:
        VITE_STARKNET_NETWORK: mainnet
    environment:
      PROVIDER_HTTP_URL: "http://execution.starknet.dncore.dappnode:6060"
    networks:
      - dncore_network
networks:
  dncore_network:
    external: true
