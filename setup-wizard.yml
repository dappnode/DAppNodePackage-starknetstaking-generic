version: "2"
fields:
  - id: op_address
    target:
      type: environment
      service: staking
      name: SIGNER_OPERATIONAL_ADDRESS
    title: Operator Address
    description: "Address used to sign attestations (Hot wallet)\ Read the [docs](https://docs.dappnode.io/docs/user/staking/starknet/solo) to learn how to get it"
    required: true
    pattern: ^0x[0-9a-fA-F]{1,64}$
    patternErrorMessage: "Invalid contract address"

  - id: priv_key
    target:
      type: environment
      service: staking
      name: SIGNER_PRIVATE_KEY
    title: Private Key
    description: "Your private key for the operator address is required to sign attestations. Keep it safe and never share it with anyone.\ Read the [docs](https://docs.dappnode.io/docs/user/staking/starknet/solo/#3%EF%B8%8F%E2%83%A3-export-your-operator-address-private-key) to learn how to get it"
    secret: true
    required: true

  - id: provider_http_url
    target:
      type: environment
      service: staking
      name: PROVIDER_HTTP_URL
    title: StarkNet Provider HTTP URL
    description: "HTTP URL of the StarkNet full node (e.g., Juno). Required for the validator to query blockchain data."
    required: true

  - id: provider_ws_url
    target:
      type: environment
      service: staking
      name: PROVIDER_WS_URL
    title: StarkNet Provider WebSocket URL
    description: "WebSocket URL of the StarkNet full node (e.g., Juno). Required for real-time blockchain updates."
    required: true

  # TODO: Try to use Web3Signer 
  # - id: external_url
  #   target:
  #     type: environment
  #     service: staking
  #     name: SIGNER_EXTERNAL_URL
  #   title: External Signer URL
  #   description: URL to external signer (if using that mode)
